version: v1beta9
deployments:
  - name: nginx
    helm:
      chart:
        name: bitnami/nginx-ingress-controller
  - name: mysql
    helm:
      chart:
        name: bitnami/mysql
      values:
        auth:
          database: florian-db
          username: florian
          rootPassword: toto123
          password: toto
        primary:
          service:
            clusterIP: 10.3.253.92
  - name: wordpress
    helm:
      chart:
        name: bitnami/wordpress
      values:
        wordpressEmail: wp@test.com
        wordpressUsername: wp
        wordpressPassword: toto
        mariadb:
          enabled: false
        externalDatabase:
          host: 10.3.253.92
          user: florian
          password: toto
          database: florian-db
          port: 3306
        autoscaling:
          minReplicas: 1
          maxReplicas: 1
        ingress:
          enabled: true
          hostname: wordpress.qaz.to
        service:
          type: ClusterIP

profiles:
- name: production
  replace:
    dependencies:
    - source:
        path: ../sealed-secrets-controller
      namespace: kube-system
